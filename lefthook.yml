pre-commit:
  parallel: true
  commands:
    lint-markdown:
      glob: "*.md"
      run: yarn lint:markdown {staged_files}
    check-markdown-links:
      skip: false
      glob: "*.md"
      run: yarn check:markdown-links {all_files}
