pre-commit:
  parallel: true
  commands:
    lint-markdown:
      glob: "*.md"
      run: yarn lint:markdown {staged_files}
    lint-markdown-links:
      skip: false
      glob: "*.md"
      run: yarn lint:markdown:links {all_files}
      # lint all_files, because renames may introduce broken links
