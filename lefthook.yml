pre-commit:
  parallel: true
  commands:
    lint-markdown:
      run: bash lint-markdown.sh
    lint-markdown-links:
      glob: "*.md"
      run: yarn lint:markdown:links . {all_files} # lint all files, because renames may introduce broken links
